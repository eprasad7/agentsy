// Stub — fully implemented in Step 2.9

export interface DevServerOptions {
  port: number;
  openBrowser: boolean;
}

export async function startDevServer(_opts: DevServerOptions): Promise<void> {
  console.log('agentsy dev — not yet implemented (Phase 2.9)');
}
