app = "agentsy-worker"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  TEMPORAL_TASK_QUEUE = "agentsy-agent-runs"

# Worker has no HTTP service — it polls Temporal

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
